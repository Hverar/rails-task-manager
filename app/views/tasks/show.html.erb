<h1><%= @task.title %></h1>

<p> <strong>Details:</strong><%= @task.details %></p>
<p> <strong>Completed:</strong><%= @task.completed %></p>

<%= link_to "Delete",
  task_path(@task),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%>
<%= link_to "Back to Home", tasks_path, class: "btn" %>
